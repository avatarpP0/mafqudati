// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model LostItem {
  id                    String    @id @default(cuid())
  title                 String
  description           String
  category              String
  location              String
  dateFound             DateTime
  imageUrl              String?
  contactName           String
  contactPhone          String
  status                String    @default("found")
  verificationQuestion  String?
  verificationAnswer    String?
  reward                String?
  latitude              Float?
  longitude             Float?
  createdAt             DateTime  @default(now())
  updatedAt             DateTime  @updatedAt
  reports               Report[]    @relation("ReportLostItem")
  favorites             Favorite[]  @relation("FavoriteLostItem")
}

model LostReport {
  id           String    @id @default(cuid())
  title        String
  description  String
  category     String
  location     String
  dateLost     DateTime
  contactName  String
  contactPhone String
  reward       String?
  status       String    @default("active")
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  reports      Report[]    @relation("ReportLostReport")
  favorites    Favorite[]  @relation("FavoriteLostReport")
}

model Report {
  id          String   @id @default(cuid())
  itemType    String   // "lostItem" or "lostReport"
  itemId      String
  reason      String   // "fake", "inappropriate", "wrong_info", "other"
  details     String?
  createdAt   DateTime @default(now())
  lostItem    LostItem?   @relation("ReportLostItem", fields: [itemId], references: [id], onDelete: Cascade)
  lostReport  LostReport? @relation("ReportLostReport", fields: [itemId], references: [id], onDelete: Cascade)
}

model Favorite {
  id          String   @id @default(cuid())
  itemType    String   // "lostItem" or "lostReport"
  itemId      String
  sessionId   String   // Browser session/fingerprint for anonymous users
  createdAt   DateTime @default(now())
  lostItem    LostItem?   @relation("FavoriteLostItem", fields: [itemId], references: [id], onDelete: Cascade)
  lostReport  LostReport? @relation("FavoriteLostReport", fields: [itemId], references: [id], onDelete: Cascade)

  @@unique([itemType, itemId, sessionId])
}
